package first.robot.opmode.utility;

import org.wpilib.opmode.PeriodicOpMode;
import org.wpilib.opmode.Utility;

import first.robot.Robot;

@Utility(name = "Tune Flywheel", group = "Tuning")
public class TuneFlywheelOpMode extends PeriodicOpMode {
  public TuneFlywheelOpMode(Robot robot) {
    // Initialize the flywheel tuning setup here
  }
}
